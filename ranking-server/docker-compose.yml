services:
  ranking:
    build: .
    container_name: pulseguard-ranking
    ports:
      - "3500:3500"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3500
      - ADMIN_USER=Admin
      - ADMIN_PASS=Admin123
      - DB_PATH=/app/data/scores.json
